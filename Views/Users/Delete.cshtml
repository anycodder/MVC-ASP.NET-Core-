@model WebApplicationTRY_TWO.Models.Users

<h2>Delete User</h2>

<h3>Are you sure you want to delete this user?</h3>
<div>
    <h4>User</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.user_name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.user_name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.user_email)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.user_email)
        </dd>
        <!-- Diğer alanlar gerekiyorsa buraya eklenebilir -->
    </dl>
    
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="id" />
        <p>Are you sure you want to delete this user?</p>
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>